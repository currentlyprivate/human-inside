node_modules
.next
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# local capture output — never commit footage
capture-out/

# private ops notes — stays on this machine
RUNBOOK.md
IDEAS.md
*.ts.tmp
*.m3u8.tmp
.env*

# compiled capture helper — rebuilt on demand
capture/.get-window-id
